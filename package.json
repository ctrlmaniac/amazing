{
  "name": "root",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "packages/*"
  ],
  "scripts": {
    "migrate": "npx lerna exec --scope=server npm run migrate",
    "dev:server": "npx lerna exec --scope=server npm run dev",
    "dev:client": "npx lerna exec --scope=client npm run dev"
  },
  "devDependencies": {
    "@types/js-cookie": "^3.0.3",
    "lerna": "^6.6.1"
  },
  "dependencies": {
    "js-cookie": "^3.0.1"
  }
}
